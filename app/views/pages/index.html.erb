<% provide(:title, "One") %>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <ul class="list-topics">
    <% @topics.each do |topic| %>
        <li class="topic"><%= topic.content %></li>
    <% end %>
    </ul>
  </div>
</div>
